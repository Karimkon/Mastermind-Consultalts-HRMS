@extends("layouts.app") @section("title","Salary") @section("content") <x-page-header title="Salary Details"><a href="{{ route('salary.index') }}" class="btn-secondary"><i class="fas fa-arrow-left"></i> Back</a></x-page-header> <div class="card p-6 max-w-md"><dl class="space-y-3">@foreach(["Employee" => $salary->employee->full_name, "Basic Salary" => "UGX " . number_format($salary->basic_salary,2), "Effective From" => $salary->effective_from->format("M d, Y")] as $label => $val)<div class="flex justify-between py-2 border-b border-slate-100"><dt class="text-sm text-slate-500">{{ $label }}</dt><dd class="text-sm font-medium text-slate-800">{{ $val }}</dd></div>@endforeach</dl></div> @endsection
